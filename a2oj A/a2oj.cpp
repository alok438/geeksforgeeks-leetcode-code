#include<bits/stdc++.h>
using namespace std;
int main()
{
	int t; cin >> t;
	while (t--) {
		string str;
		cin >> str;
		int n = str.size();
		for (int i = 0; i < n; i++)
			if (n > 10)
				cout << s[0] << n - 2 << s[n - 1];
		cout << endl;
	}
}
